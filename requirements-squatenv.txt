# SquatEnv — legacy MediaPipe Solutions API pipeline (squat only).
# Versions below match the environment the squat pipeline was built
# and validated against (see squat_final.ipynb environment printout).
mediapipe==0.10.14
opencv-python==4.13.0.92
numpy==2.2.6
pandas==2.3.1
scikit-learn==1.7.2
protobuf==4.25.3
